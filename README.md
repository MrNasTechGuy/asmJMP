# asmJMP 
asmJMP is a ti 84 evo Poc, it is Not a full exploit chain but can be used to gain code execution on the Evo via a asm exploit.

# Ti-Nspire
the Nspire cx 2 cannot run this exploit as it needs another bypass to work.